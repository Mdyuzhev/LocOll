# Команда /init — инициализация сессии

Сразу написать: **🚀 Начинаю работу**

Вызвать `start_session` с `project_path` = текущая рабочая директория.

Вывести полученный контекст полностью — он уже содержит статус сервера,
метрики, контейнеры, события и заметки.

→ написать: **✅ Готово**
