version: "3.9"

services:
  frontend:
    build: ./frontend
    container_name: locoll-frontend
    ports:
      - "8090:80"
    extra_hosts:
      - "ollama-host:host-gateway"
    restart: unless-stopped
